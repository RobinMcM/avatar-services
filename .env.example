# Avatar Services Environment Configuration
# Copy this file to .env and update with your values
#
# Quick setup:
# 1. cp .env.example .env
# 2. Get your server IP: curl -4 ifconfig.me
# 3. Edit .env and replace YOUR_DROPLET_IP_HERE with your actual IP
# 4. Run: docker compose up -d

# Your droplet or server IP address (required for audio serving)
DROPLET_IP=YOUR_DROPLET_IP_HERE

# Audio serving configuration (uses DROPLET_IP variable)
# This URL is returned in API responses for the browser to load audio files
AUDIO_BASE_URL=http://${DROPLET_IP}:8080/audio

# Valkey (Redis-compatible) configuration
VALKEY_HOST=valkey
VALKEY_PORT=6379

# Application settings
DATA_DIR=/data
LOG_LEVEL=INFO
MAX_CONCURRENT_REQUESTS=10
SYNC_TIMEOUT_SECONDS=60

# Worker settings
PIPER_MODEL=/opt/piper/voices/en_US-lessac-medium.onnx
